{
  "name": "pi-sheepdog",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Pi extension that detects provider rate-limit/quota errors and schedules a follow-up message to resume once the cooldown passes.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "rate-limit",
    "wakeup",
    "sheepdog"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tigorlazuardi/pi-sheepdog.git"
  },
  "files": [
    "extensions",
    "README.md",
    "docs/astro.config.mjs",
    "docs/package.json",
    "docs/package-lock.json",
    "docs/tsconfig.json",
    "docs/src"
  ],
  "scripts": {
    "docs:dev": "npm --prefix docs run dev",
    "docs:build": "sh -c '[ -x docs/node_modules/.bin/astro ] || npm --prefix docs ci --no-audit --no-fund; npm --prefix docs run build'",
    "self-check": "node --experimental-strip-types ./scripts/self-check.mjs",
    "typecheck": "tsc",
    "check": "npm run typecheck && npm run docs:build && npm pack --dry-run"
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.7",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
