---
title: Configuration
description: Config and state paths for the dedicated Sheepdog identity.
status: current
---

# Configuration

## Paths

Sheepdog resolves the Pi agent directory as:

```ts
process.env.PI_CODING_AGENT_DIR ?? path.join(os.homedir(), '.pi', 'agent')
```

Expected paths:

- Config: `${agentDir}/sheepdog/config.json`
- State: `${agentDir}/.cache/sheepdog/state.json`
- Debug log: `${agentDir}/sheepdog/debug.log`

## Ticket 1 note

Ticket 1 only rebrands the config location and command surface.

Later tickets add config creation, mapper rules, adapter args, and validation.
