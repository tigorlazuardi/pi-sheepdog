# T5 merge resolution

Resolved T3/T5 conflicts. Preserved mapper config/types and mapper-selected adapter on automatic entries, plus v3 state status/origin/atomic locked updates and state self-check mode.

Checks: `npm run self-check -- state && npm run check`.
