extensions/sheepdog.ts:516 — high: State persistence still writes directly to the final JSON path with no lock/temp-file/rename sequence, which violates CODING_STANDARDS.md "State and concurrency" and can leave a truncated/corrupt state file when two Sheepdog processes save concurrently or the process dies mid-write. Change saveState() to take a short lock, write to a temp file in the same directory, then rename atomically onto state.json.
